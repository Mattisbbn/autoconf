export default function Footer() {
    return (
        <footer className=" mt-auto mb-2">
            <p className="text-sm font-medium text-gray-500 text-center">© {new Date().getFullYear()} Autoconf</p>
        </footer>
    )
}